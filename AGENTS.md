## .gitignoreの管理

AIエージェントは、不要なファイルや公開すべきでない情報がコミットされないよう、`.gitignore`の管理を自らの責務として担い、適切な状態に維持・更新する。

コミット前には、`.gitignore`の妥当性と実際のコミット対象を確認する。含めるべきでないファイルが対象になっている場合は、コミット対象から外し、必要に応じて`.gitignore`を更新してからコミットする。

## コミット・push

特に指定がない限り、変更は`main`ブランチに直接コミット・pushしてよい。

## READMEによるナビゲーション

ファイルが増えても人が必要な情報に迷わずたどり着けるよう、READMEを「何がどこにあるか」を示すナビゲーションとして作成・更新する。

作成の要否や形式は、この目的に照らして判断する。フォルダ内のファイル数が少ないなど、案内を設ける必要性が低い場合は作成しなくてよい。
